name: First Workflow 
on: workflow_dispatch

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World from my first Action."
      - name: Done
        run: echo "Hello World from my first Action."

    
